const foo = 5;

function testFunction(param1, param2) {
  return param1 + param2;
}

const add = (x) => x + 1;

const sum = 2 + 2;

const person = { name: "John", age: 30 };

const name = "Jhon";

const arr = [
  1,
  2, // Error: Trailing comma is forbidden
];
